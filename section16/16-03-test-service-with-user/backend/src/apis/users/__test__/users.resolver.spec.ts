describe('UsersResolver', () => {
  beforeAll(() => {
    // 로그인 등 공통 기능
  });

  describe('fetchUser', () => {
    it('유저 정보 꺼내오기', () => {
      // 컨트롤러 테스트랑 같은 방식으로 테스트 하기 !!
    });
  });

  describe('createUser', () => {
    it('유저 등록하기', () => {
      // 컨트롤러 테스트랑 같은 방식으로 테스트 하기 !!
    });
  });
});
